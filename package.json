{
  "name": "diana-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Diana configuration language tooling",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxlint": "^1.1.0"
  }
}
