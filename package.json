{
  "name": "diana-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for Diana programming language tooling",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "oxlint": "^1.1.0"
  }
}
